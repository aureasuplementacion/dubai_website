import Link from "next/link";
import { ButtonLink } from "@/components/ui/button";
import { bhtClinic } from "@/lib/clinics/data";
import { getSpecialty } from "@/lib/catalog/data";

export default async function ClinicsPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const english = locale === "en";
  const language = english ? "en" : "es";
  return <main className="container-shell py-16"><Link className="inline-flex min-h-11 items-center text-sm font-semibold text-sapphire" href={`/${locale}`}>← Aura Estética</Link><div className="reveal mt-12 max-w-3xl"><p className="eyebrow">{english ? "Our partner hospital" : "Nuestro hospital partner"}</p><h1 className="mt-4 font-display text-5xl text-sapphire">{english ? "A real centre for your journey" : "Un centro real para tu viaje"}</h1><p className="mt-5 text-lg leading-8 text-muted">{english ? "Explore the verified information available about BHT CLINIC Istanbul Tema Hospital before speaking with an advisor." : "Consulta la información verificada disponible sobre BHT CLINIC Istanbul Tema Hospital antes de hablar con un asesor."}</p></div><article className="mt-12 overflow-hidden rounded-[2rem] border border-border bg-white shadow-soft"><div className="surface-grid bg-sapphire p-8 text-white md:p-12"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="eyebrow eyebrow-on-dark">{english ? "Verified information" : "Información verificada"}</p><h2 className="mt-3 font-display text-4xl">{bhtClinic.name}</h2><p className="mt-3 text-white/75">{bhtClinic.cityLabel[language]} · {english ? "International patient centre" : "Unidad de pacientes internacionales"}</p></div><span className="rounded-full border border-white/25 px-4 py-2 text-sm text-white/85">{english ? "Updated 19 Aug 2026" : "Actualizado el 19 ago 2026"}</span></div><p className="mt-7 max-w-3xl text-lg leading-8 text-white/80">{bhtClinic.description[language]}</p><ButtonLink className="mt-8 bg-white text-sapphire hover:bg-champagne-light" href={`/${locale}/clinicas/${bhtClinic.slug}`}>{english ? "View centre profile" : "Ver ficha del centro"}</ButtonLink></div><div className="grid gap-8 p-8 md:grid-cols-2 md:p-10"><div><p className="eyebrow">{english ? "Published areas" : "Áreas publicadas"}</p><div className="mt-4 flex flex-wrap gap-2">{bhtClinic.specialties.map((slug) => <span key={slug} className="rounded-full bg-surface px-4 py-2 text-sm font-semibold text-sapphire">{getSpecialty(slug)?.name[language] || slug}</span>)}</div></div><div><p className="eyebrow">{english ? "International coordination" : "Coordinación internacional"}</p><p className="mt-3 leading-7 text-muted">{english ? "Coordinator, interpreter, accommodation and private transfer options are confirmed case by case in the written proposal." : "Coordinador, intérprete, alojamiento y opciones de traslado privado se confirman caso por caso en la propuesta escrita."}</p></div></div></article><div className="mt-12 flex flex-wrap items-center justify-between gap-5 rounded-[1.5rem] bg-surface p-7"><div><p className="eyebrow">{english ? "Need help understanding the options?" : "¿Necesitas ayuda para entender las opciones?"}</p><p className="mt-2 font-display text-2xl text-sapphire">{english ? "An advisor can guide your next conversation." : "Un asesor puede ayudarte a preparar la siguiente conversación."}</p></div><ButtonLink href={`/${locale}/reservar`}>{english ? "Talk to an advisor" : "Hablar con un asesor"}</ButtonLink></div></main>;
}
