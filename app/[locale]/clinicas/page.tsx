import Image from "next/image";
import Link from "next/link";
import { ButtonLink } from "@/components/ui/button";
import { bhtClinic } from "@/lib/clinics/data";
import { getSpecialty } from "@/lib/catalog/data";
import { media } from "@/lib/media/registry";

export default async function ClinicsPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params; const english = locale === "en"; const language = english ? "en" : "es";
  return <main><section className="bg-deep text-white"><div className="container-shell grid gap-10 py-16 md:grid-cols-[.9fr_1.1fr] md:items-end md:py-24"><div><Link className="inline-flex min-h-11 items-center text-sm font-semibold text-white/70 hover:text-white" href={`/${locale}`}>← Aura Estética</Link><p className="eyebrow eyebrow-on-dark mt-14">{english ? "Partner centre" : "Centro partner"}</p><h1 className="mt-4 max-w-3xl font-display text-6xl font-semibold leading-[.95] md:text-7xl">{english ? "A real centre for your journey." : "Un centro real para tu viaje."}</h1></div><p className="max-w-xl text-lg leading-8 text-white/70">{english ? "Explore the published information available about BHT CLINIC Istanbul Tema Hospital before speaking with an advisor." : "Consulta la información publicada disponible sobre BHT CLINIC Istanbul Tema Hospital antes de hablar con un asesor."}</p></div></section><section className="container-shell py-16 md:py-24"><article className="overflow-hidden rounded-[2rem] border border-border bg-canvas shadow-soft"><div className="grid md:grid-cols-[1.1fr_.9fr]"><div className="relative min-h-[24rem] md:min-h-[34rem]"><Image src={media.partners.exterior.desktop} alt={media.partners.exterior.alt[language]} fill priority sizes="(max-width: 768px) 100vw, 55vw" className="object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-deep/75 to-transparent" /><div className="absolute bottom-7 left-7 right-7 text-white md:bottom-10 md:left-10"><p className="eyebrow eyebrow-on-dark">{english ? "Representative partner image" : "Imagen representativa del partner"}</p><h2 className="mt-3 font-display text-4xl font-semibold">BHT CLINIC</h2></div></div><div className="flex flex-col justify-center p-8 md:p-12"><p className="eyebrow">{english ? "Published information" : "Información publicada"}</p><h2 className="mt-3 font-display text-4xl font-semibold text-sapphire">{bhtClinic.name}</h2><p className="mt-3 text-sm text-muted">{bhtClinic.cityLabel[language]} · {english ? "International patient centre" : "Unidad de pacientes internacionales"}</p><p className="mt-7 leading-8 text-muted">{bhtClinic.description[language]}</p><ButtonLink className="mt-8 self-start" href={`/${locale}/clinicas/${bhtClinic.slug}`}>{english ? "View centre profile" : "Ver ficha del centro"}</ButtonLink></div></div><div className="grid gap-8 border-t border-border p-8 md:grid-cols-2 md:p-10"><div><p className="eyebrow">{english ? "Published areas" : "Áreas publicadas"}</p><div className="mt-4 flex flex-wrap gap-2">{bhtClinic.specialties.map((slug) => <span key={slug} className="rounded-full bg-surface px-4 py-2 text-sm font-semibold text-sapphire">{getSpecialty(slug)?.name[language] || slug}</span>)}</div></div><div><p className="eyebrow">{english ? "International coordination" : "Coordinación internacional"}</p><p className="mt-3 leading-7 text-muted">{english ? "Coordinator, interpreter, accommodation and private transfer options are confirmed case by case in the written proposal." : "Coordinador, intérprete, alojamiento y opciones de traslado privado se confirman caso por caso en la propuesta escrita."}</p></div></div></article><div className="mt-8 grid gap-5 md:grid-cols-2"><div className="image-frame relative min-h-[18rem]"><Image src={media.partners.international.desktop} alt={media.partners.international.alt[language]} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-deep/75 to-transparent" /><p className="absolute bottom-6 left-6 font-display text-3xl font-semibold text-white">{english ? "International context" : "Contexto internacional"}</p></div><div className="image-frame relative min-h-[18rem]"><Image src={media.partners.team.desktop} alt={media.partners.team.alt[language]} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-deep/75 to-transparent" /><p className="absolute bottom-6 left-6 font-display text-3xl font-semibold text-white">{english ? "Human coordination" : "Coordinación humana"}</p></div></div><div className="mt-12 flex flex-wrap items-center justify-between gap-5 rounded-[2rem] bg-surface p-8 md:p-10"><div><p className="eyebrow">{english ? "Need help understanding the options?" : "¿Necesitas ayuda para entender las opciones?"}</p><p className="mt-2 font-display text-3xl font-semibold text-sapphire">{english ? "An advisor can guide your next conversation." : "Un asesor puede ayudarte a preparar la siguiente conversación."}</p></div><ButtonLink href={`/${locale}/reservar`}>{english ? "Talk to an advisor" : "Hablar con un asesor"}</ButtonLink></div></section></main>;
}
