import Link from "next/link";
import { publishedSpecialties } from "@/lib/catalog/data";
import { ButtonLink } from "@/components/ui/button";

export default async function SpecialtiesPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params; const english = locale === "en"; const language = english ? "en" : "es";
  return <main className="container-shell py-16"><Link className="inline-flex min-h-11 items-center text-sm font-semibold text-sapphire" href={`/${locale}`}>← Aura Estética</Link><div className="mt-12 max-w-3xl"><p className="eyebrow">Aura Estética</p><h1 className="mt-4 font-display text-5xl text-sapphire">{english ? "A directory to inform your decision" : "Un directorio para informarte"}</h1><p className="mt-4 text-lg leading-8 text-muted">{english ? "Explore the areas currently published for the BHT CLINIC international pathway. Availability and suitability are confirmed case by case." : "Explora las áreas publicadas actualmente para el itinerario internacional con BHT CLINIC. La disponibilidad y adecuación se confirman caso por caso."}</p></div><div className="mt-14 divide-y divide-border border-y border-border">{publishedSpecialties.map((specialty) => <section key={specialty.slug} className="grid gap-6 py-9 md:grid-cols-[.75fr_1.25fr] md:gap-12"><div><p className="eyebrow">{english ? "Specialty" : "Especialidad"}</p><h2 className="mt-2 font-display text-3xl text-sapphire"><Link href={`/${locale}/especialidades/${specialty.slug}`}>{specialty.name[language]}</Link></h2><p className="mt-3 text-sm leading-6 text-muted">{specialty.shortDescription[language]}</p></div><div className="grid gap-x-8 gap-y-5 sm:grid-cols-2">{specialty.services.map((service) => <div key={service.slug}><Link href={`/${locale}/especialidades/${specialty.slug}/${service.slug}`} className="font-semibold text-sapphire">{service.name[language]}</Link><p className="mt-1 text-xs leading-5 text-muted">{service.options.map((item) => item.name[language]).join(" · ")}</p></div>)}</div></section>)}</div><div className="mt-12 flex flex-wrap items-center justify-between gap-5 rounded-[1.5rem] bg-surface p-7"><div><p className="eyebrow">{english ? "The process starts before you travel" : "El proceso comienza antes de viajar"}</p><p className="mt-2 font-display text-2xl text-sapphire">{english ? "Understand the journey from Spain to your return home." : "Conoce el viaje completo, desde España hasta tu regreso a casa."}</p></div><ButtonLink href={`/${locale}/como-funciona`}>{english ? "Start your journey" : "Empieza tu viaje"}</ButtonLink></div></main>;
}
