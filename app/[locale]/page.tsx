import Link from "next/link";
import Image from "next/image";
import { getTranslations } from "next-intl/server";
import { SpecialtyCarousel } from "@/components/home/specialty-carousel";
import { TravelTimeline } from "@/components/home/travel-timeline";
import { ButtonLink } from "@/components/ui/button";
import { getSpecialty } from "@/lib/catalog/data";
import { demoClinics } from "@/lib/clinics/data";

export default async function HomePage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations("Home");
  const language = locale as "es" | "en";
  const english = locale === "en";
  const featuredClinics = demoClinics.slice(0, 3);

  return <main>
    <section className="surface-grid bg-surface"><div className="container-shell grid min-h-[560px] items-center gap-12 py-14 md:grid-cols-[.9fr_1.1fr] md:py-20"><div className="reveal order-2 md:order-1"><p className="eyebrow mb-5">{t("eyebrow")}</p><h1 className="max-w-2xl font-display text-5xl leading-[1.05] text-sapphire md:text-7xl">{t("title")}</h1><p className="mt-7 max-w-xl text-lg leading-8 text-muted">{t("description")}</p><div className="mt-9 flex flex-wrap gap-4"><Link className="inline-flex min-h-12 items-center justify-center rounded-full bg-sapphire px-6 py-4 font-semibold text-white shadow-soft transition duration-base ease-standard hover:bg-sapphire-dark" href={`/${locale}/reservar`}>{t("primaryCta")}</Link><Link className="inline-flex min-h-12 items-center justify-center rounded-full border border-sapphire bg-white px-6 py-4 font-semibold text-sapphire transition duration-base ease-standard hover:bg-surface" href={`/${locale}/especialidades`}>{t("secondaryCta")}</Link></div></div><div className="relative order-1 min-h-[420px] overflow-hidden rounded-[2rem] shadow-soft md:order-2"><Image src="/images/aura-hero-consultation.png" alt={t("heroAlt")} fill priority sizes="(max-width: 768px) 100vw, 55vw" className="object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-sapphire/70 via-transparent to-transparent" /><div className="absolute bottom-6 left-6 right-6 rounded-2xl border border-white/30 bg-white/90 p-6 backdrop-blur"><p className="eyebrow">{t("cardEyebrow")}</p><p className="mt-3 font-display text-2xl text-sapphire">{t("cardTitle")}</p><p className="mt-2 text-sm leading-6 text-muted">{t("cardDescription")}</p></div></div></div></section>
    <section className="border-b border-border bg-white"><div className="container-shell grid gap-5 py-7 text-sm md:grid-cols-4"><div><p className="font-semibold text-sapphire">{english ? "Spain to Türkiye" : "España hacia Turquía"}</p><p className="mt-1 text-muted">{english ? "Coordination from the first call" : "Coordinación desde la primera llamada"}</p></div><div><p className="font-semibold text-sapphire">{english ? "Verified information" : "Información verificable"}</p><p className="mt-1 text-muted">{english ? "Centres and professionals to review" : "Centros y profesionales para conocer"}</p></div><div><p className="font-semibold text-sapphire">{english ? "Human support" : "Acompañamiento humano"}</p><p className="mt-1 text-muted">{english ? "An advisor remains your contact" : "Un asesor es tu punto de contacto"}</p></div><div><p className="font-semibold text-sapphire">{english ? "Clinical clarity" : "Claridad clínica"}</p><p className="mt-1 text-muted">{english ? "Subject to medical assessment" : "Sujeto a valoración médica"}</p></div></div></section>
    <section className="container-shell py-20"><div className="flex flex-wrap items-end justify-between gap-6"><div><p className="eyebrow">{english ? "Explore your options" : "Explora tus opciones"}</p><h2 className="mt-3 font-display text-4xl text-sapphire">{t("specialtiesTitle")}</h2><p className="mt-3 max-w-xl text-muted">{t("specialtiesDescription")}</p></div><Link className="inline-flex min-h-11 items-center font-semibold text-sapphire underline decoration-champagne decoration-2 underline-offset-8 transition hover:text-sapphire-dark" href={`/${locale}/especialidades`}>{t("viewAll")}</Link></div><SpecialtyCarousel locale={locale} /></section>
    <section className="bg-surface py-20"><div className="container-shell"><div className="max-w-2xl"><p className="eyebrow">{english ? "The journey" : "El viaje"}</p><h2 className="mt-3 font-display text-4xl text-sapphire">{english ? "Know what happens at every stage" : "Conoce qué ocurre en cada etapa"}</h2><p className="mt-4 text-lg leading-8 text-muted">{english ? "From your first conversation in Spain to your return home, we make the operational path easier to understand." : "Desde tu primera conversación en España hasta tu regreso a casa, hacemos más comprensible el recorrido operativo."}</p></div><TravelTimeline locale={locale} /><ButtonLink tone="ghost" className="mt-8 px-0" href={`/${locale}/como-funciona`}>{english ? "See the complete journey" : "Ver el viaje completo"}</ButtonLink></div></section>
    <section className="container-shell py-20"><div className="flex flex-wrap items-end justify-between gap-6"><div><p className="eyebrow">{english ? "Our demo network" : "Nuestra red demo"}</p><h2 className="mt-3 font-display text-4xl text-sapphire">{english ? "Get to know the centres and teams" : "Conoce los centros y equipos"}</h2><p className="mt-3 max-w-xl text-muted">{english ? "Explore the type of information you will be able to review before speaking with an advisor." : "Explora el tipo de información que podrás revisar antes de hablar con un asesor."}</p></div><Link className="inline-flex min-h-11 items-center font-semibold text-sapphire underline decoration-champagne decoration-2 underline-offset-8" href={`/${locale}/clinicas`}>{english ? "View the network" : "Ver la red"}</Link></div><div className="mt-10 grid gap-5 md:grid-cols-3">{featuredClinics.map((clinic) => <article key={clinic.slug} className="rounded-[1.25rem] border border-border bg-white p-6 transition hover:-translate-y-1 hover:shadow-soft"><div className="flex items-start justify-between gap-3"><p className="eyebrow">{english ? "Demo profile" : "Perfil demo"}</p><span className="rounded-full bg-champagne-light px-3 py-1 text-xs font-semibold text-sapphire">{clinic.cityLabel[language]}</span></div><h3 className="mt-3 font-display text-2xl text-sapphire">{clinic.name}</h3><p className="mt-3 text-sm leading-6 text-muted">{clinic.description[language]}</p><div className="mt-5 flex flex-wrap gap-2">{clinic.specialties.map((slug) => <span key={slug} className="rounded-full bg-surface px-3 py-1 text-xs font-semibold text-sapphire">{getSpecialty(slug)?.name[language] || slug}</span>)}</div><Link className="mt-6 inline-flex min-h-11 items-center font-semibold text-sapphire underline decoration-champagne underline-offset-4" href={`/${locale}/clinicas/${clinic.slug}`}>{english ? "View profile" : "Ver ficha"}</Link></article>)}</div></section>
    <section className="bg-sapphire py-20 text-white"><div className="container-shell grid gap-8 md:grid-cols-2"><div><p className="eyebrow eyebrow-on-dark">Aura Estética</p><h2 className="mt-4 font-display text-4xl">{t("trustTitle")}</h2></div><p className="max-w-md self-end text-lg leading-8 text-white/75">{t("trustDescription")}</p></div><div className="container-shell mt-10"><ButtonLink className="bg-white text-sapphire hover:bg-champagne-light" href={`/${locale}/reservar`}>{english ? "Talk to an advisor" : "Hablar con un asesor"}</ButtonLink></div></section>
  </main>;
}
