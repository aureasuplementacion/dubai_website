import Link from "next/link";

export function PublicFooter({ locale }: { locale: string }) {
  const isEnglish = locale === "en";
  return <footer className="bg-deep text-white"><div className="container-shell grid gap-12 py-16 md:grid-cols-[1.35fr_.65fr_.65fr]"><div><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full border border-champagne text-xs font-bold text-champagne">AE</span><Link href={`/${locale}`} className="font-display text-3xl font-semibold">Aura <span className="text-champagne">Estética</span></Link></div><p className="mt-5 max-w-sm text-sm leading-7 text-white/65">{isEnglish ? "Clear information and independent referral to healthcare providers in Türkiye." : "Información clara y contacto con proveedores sanitarios independientes en Turquía."}</p></div><div><p className="eyebrow eyebrow-on-dark">{isEnglish ? "Explore" : "Explora"}</p><div className="mt-5 grid gap-3 text-sm text-white/70"><Link href={`/${locale}/especialidades`} className="transition hover:text-white">{isEnglish ? "Specialties" : "Especialidades"}</Link><Link href={`/${locale}/clinicas`} className="transition hover:text-white">{isEnglish ? "Healthcare centres" : "Centros sanitarios"}</Link><Link href={`/${locale}/como-funciona`} className="transition hover:text-white">{isEnglish ? "How it works" : "Cómo funciona"}</Link></div></div><div><p className="eyebrow eyebrow-on-dark">{isEnglish ? "Start a conversation" : "Empieza una conversación"}</p><div className="mt-5 grid gap-3 text-sm text-white/70"><Link href={`/${locale}/reservar`} className="transition hover:text-white">{isEnglish ? "Request contact" : "Solicitar contacto"}</Link><Link href={`/${locale}/contacto`} className="transition hover:text-white">WhatsApp / Email</Link></div></div></div><div className="border-t border-white/10"><div className="container-shell flex flex-wrap justify-between gap-3 py-5 text-xs text-white/45"><span>© 2026 Aura Estética</span><span>{isEnglish ? "General information; healthcare belongs to the responsible provider." : "Información general; la asistencia corresponde al proveedor responsable."}</span></div></div></footer>;
}
